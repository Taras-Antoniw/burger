<h1>Burgers!</h1>

<h2>Selection of Delicious Burgers!</h2>

<ul>
  {{#each burgers}}
    {{#unless devoured}}
      {{> burgers/burger-tryme devoured=true}}
    {{/unless}}
  {{/each}}
</ul>

<h2>Too bad so sad - These are gone!</h2>

<ul>
  {{#each burgers}}
    {{#if devoured}}
     {{> burgers/burger-eaten devoured=false}}
    {{/if}}
  {{/each}}
</ul>

<h2>Add a Burger</h2>
<form class="create-form">

  <div class="form-group">
    <label for="burg">Burger Name:</label>
    <input type="text" id="burg" name="name">
  </div>


  <button type="submit">Add a Burger</button>
</form>
